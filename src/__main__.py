"""Module providing a function to extract the audio from a video and export"""
# main
# /usr/bin/env python3

# from .modules.audio import extract_audio, transcribe_audio_to_text
# from .modules.video import download_channel, download_playlist, download_video


def main():
    '''
     Extract the audio from a video file  and save to an MP3 to the
     specified directory.

     Args:
     video (str): The name of the video file to be converted.
     audio (str): The name of the resulting MP3 audio file.

     Returns:
     None: The function does not return any object, just a success or error
      message.
    '''

    # Define the input video file name and output audio file
    # extract_audio(video="video.mp4", audio="audio.wav")
    # download_video("")
    # download_playlist("")
    # download_channel("")
    # transcribe_audio_to_text("interessante.wav")


if __name__ == "__main__":
    main()
